#include <stdio.h>
#include <stdbool.h>

int main()
{
    int n;

    printf("Enter number of processes: ");
    scanf("%d", &n);

    int pid[n], at[n], bt[n], pr[n];
    int ct[n], tat[n], wt[n];
    bool completed[n];

    for(int i = 0; i < n; i++)
    {
        printf("Enter ProcessID, Arrival Time, Burst Time, Priority: ");
        scanf("%d %d %d %d", &pid[i], &at[i], &bt[i], &pr[i]);
        completed[i] = false;
    }

    int current_time = 0;
    int completed_count = 0;

    while(completed_count < n)
    {
        int highest_priority = 9999;
        int selected_process = -1;

        for(int i = 0; i < n; i++)
        {
            if(at[i] <= current_time && completed[i] == false)
            {
                if(pr[i] < highest_priority)
                {
                    highest_priority = pr[i];
                    selected_process = i;
                }
            }
        }

        if(selected_process == -1)
        {
            current_time++;
        }
        else
        {
            int start_time = current_time;

            ct[selected_process] = start_time + bt[selected_process];

            tat[selected_process] = ct[selected_process] - at[selected_process];

            wt[selected_process] = tat[selected_process] - bt[selected_process];

            current_time = ct[selected_process];

            completed[selected_process] = true;
            completed_count++;
        }
    }

    printf("\nPID\tAT\tBT\tPR\tWT\tTAT\n");

    for(int i = 0; i < n; i++)
    {
        printf("%d\t%d\t%d\t%d\t%d\t%d\n",
               pid[i], at[i], bt[i], pr[i], wt[i], tat[i]);
    }

    return 0;
}


OUTPUT:

1WA24CS201

Enter number of processes: 5
Enter ProcessID, Arrival Time, Burst Time, Priority: P1 0 3 5
Enter ProcessID, Arrival Time, Burst Time, Priority: P2 2 2 3
Enter ProcessID, Arrival Time, Burst Time, Priority: P3 3 5 2
Enter ProcessID, Arrival Time, Burst Time, Priority: P4 4 4 4
Enter ProcessID, Arrival Time, Burst Time, Priority: P5 6 1 1

PID      AT      BT      PR      WT      TAT
P1       0       3       5       0       3
P2       2       2       3       7       9
P3       3       5       2       0       5
P4       4       4       4       7       11
P5       6       1       1       2       3

Process returned 0 (0x0)   execution time : 35.161 s
Press any key to continue.
